---
layout: default
title: 404 — Page Not Found
permalink: /404.html
---

<style>
.not-found { text-align: center; padding: 3rem 0 5rem; }

/* Glitch 404 */
.error-code {
  font-size: clamp(6rem, 18vw, 10rem);
  font-weight: 700;
  color: var(--bg-4);
  line-height: 1;
  letter-spacing: -0.04em;
  position: relative;
  user-select: none;
  cursor: pointer;
}
.error-code::before,
.error-code::after {
  content: '404';
  position: absolute;
  inset: 0;
  display: flex;
  align-items: center;
  justify-content: center;
}
.error-code::before {
  color: var(--purple);
  animation: glitch-1 2.8s infinite;
  clip-path: polygon(0 0, 100% 0, 100% 35%, 0 35%);
}
.error-code::after {
  color: var(--blue);
  animation: glitch-2 2.8s infinite;
  clip-path: polygon(0 65%, 100% 65%, 100% 100%, 0 100%);
}

@keyframes glitch-1 {
  0%, 90%, 100% { transform: translate(0); }
  91%  { transform: translate(-4px, 1px); }
  93%  { transform: translate(4px, -1px); }
  95%  { transform: translate(-3px, 2px); }
  97%  { transform: translate(3px, -2px); }
}
@keyframes glitch-2 {
  0%, 85%, 100% { transform: translate(0); }
  86%  { transform: translate(4px, 2px); }
  88%  { transform: translate(-4px, -2px); }
  90%  { transform: translate(3px, 1px); }
  92%  { transform: translate(-3px, -1px); }
}

/* Panic subtitle */
.panic-line {
  font-family: var(--font-mono);
  font-size: 0.72rem;
  color: var(--text-3);
  letter-spacing: 0.12em;
  text-transform: uppercase;
  margin: 0.5rem 0 1rem;
  animation: flicker 4s infinite;
}
@keyframes flicker {
  0%, 95%, 100% { opacity: 1; }
  96% { opacity: 0.3; }
  97% { opacity: 1; }
  98% { opacity: 0.5; }
  99% { opacity: 1; }
}

/* Memory dump scroll */
.mem-dump {
  font-family: var(--font-mono);
  font-size: 0.62rem;
  color: var(--bg-4);
  letter-spacing: 0.05em;
  margin: 0.5rem 0 1.5rem;
  white-space: nowrap;
  overflow: hidden;
}
.mem-inner {
  display: inline-block;
  animation: mem-scroll 12s linear infinite;
}
@keyframes mem-scroll {
  from { transform: translateX(0); }
  to   { transform: translateX(-50%); }
}

/* Shake on click */
@keyframes shake {
  0%, 100% { transform: translateX(0); }
  20%  { transform: translateX(-6px); }
  40%  { transform: translateX(6px); }
  60%  { transform: translateX(-4px); }
  80%  { transform: translateX(4px); }
}
.shaking { animation: shake 0.4s ease; }

/* Nav buttons */
.nav-buttons {
  display: flex;
  flex-wrap: wrap;
  gap: 0.6rem;
  justify-content: center;
  margin-top: 1.5rem;
}
</style>

<div class="not-found">

  <div class="error-code" id="e404">404</div>

  <div class="panic-line">// kernel panic — page not found — not a drill</div>

  <!-- Scrolling memory dump -->
  <div class="mem-dump">
    <span class="mem-inner">
      0x0000 0x4E45 0x5457 0x4F52 0x4B45 0x434C 0x4950 0x5345 0xDEAD 0xBEEF 0xC0DE 0xBAD0 0xFF04 0x0404 0x0000 &nbsp;&nbsp;
      0x0000 0x4E45 0x5457 0x4F52 0x4B45 0x434C 0x4950 0x5345 0xDEAD 0xBEEF 0xC0DE 0xBAD0 0xFF04 0x0404 0x0000
    </span>
  </div>

  <!-- Crash terminal -->
  <div class="terminal" style="max-width: 520px; margin: 0 auto 1.5rem; text-align: left;">
    <div class="terminal-bar">
      <div class="terminal-dots">
        <span class="dot-red"></span><span class="dot-amber"></span><span class="dot-green"></span>
      </div>
      <div class="terminal-title">bash — SIGSEGV — core dumped</div>
    </div>
    <div class="terminal-body" id="crash-log">
      <div><span class="t-comment">## KERNEL PANIC — networkeclipse.com</span></div>
    </div>
  </div>

  <!-- Nav links -->
  <div class="nav-buttons">
    <a href="{{ '/' | relative_url }}" class="badge badge-purple" style="font-size:0.8rem; padding:7px 16px; border-radius:8px;">← ~/home</a>
    <a href="{{ '/blog/' | relative_url }}" class="badge badge-gray" style="font-size:0.8rem; padding:7px 16px; border-radius:8px;">📓 lab-notes</a>
    <a href="{{ '/projects/' | relative_url }}" class="badge badge-gray" style="font-size:0.8rem; padding:7px 16px; border-radius:8px;">🛠 stack</a>
    <a href="{{ '/about/' | relative_url }}" class="badge badge-gray" style="font-size:0.8rem; padding:7px 16px; border-radius:8px;">~$ whoami</a>
  </div>

</div>

<script>
(function() {
  var log = document.getElementById('crash-log');
  var lines = [
    { d: 400, cls: 't-prompt', t: 'freaky@networkeclipse:~$ curl ' + window.location.pathname },
    { d: 600, cls: 't-err',    t: 'Segmentation fault (core dumped)' },
    { d: 300, cls: 't-comment',t: '## attempting recovery...' },
    { d: 500, cls: 't-prompt', t: 'freaky@networkeclipse:~$ find / -type d -name ' + window.location.pathname.substring(1) },
    { d: 700, cls: 't-err',    t: 'find: no results — 0 matches — gave up' },
    { d: 300, cls: 't-comment',t: '## memory dump:  0xDEAD 0xBEEF 0x0404' },
    { d: 400, cls: 't-prompt', t: 'freaky@networkeclipse:~$ ping hope' },
    { d: 500, cls: 't-err',    t: 'ping: hope: Name or service not known' },
    { d: 300, cls: 't-comment',t: '## redirecting to known-good destinations:' },
    { d: 200, cls: 't-ok',     t: '\u2192 / (home) .............. [REACHABLE]' },
    { d: 200, cls: 't-ok',     t: '\u2192 /blog/ (lab-notes) ... [REACHABLE]' },
    { d: 200, cls: 't-ok',     t: '\u2192 /projects/ (stack) ... [REACHABLE]' },
    { d: 200, cls: 't-ok',     t: '\u2192 /about/ (whoami) ..... [REACHABLE]' },
    { d: 400, cls: 't-prompt', t: 'freaky@networkeclipse:~$ ' },
  ];

  var i = 0;
  function typeNext() {
    if (i >= lines.length) return;
    var l = lines[i++];
    setTimeout(function() {
      var div = document.createElement('div');
      div.className = l.cls;
      div.textContent = '';
      log.appendChild(div);
      var c = 0;
      var speed = l.cls === 't-prompt' ? 28 : 10;
      var iv = setInterval(function() {
        div.textContent += l.t[c++];
        if (c >= l.t.length) {
          clearInterval(iv);
          if (i === lines.length) {
            var cur = document.createElement('span');
            cur.className = 't-cursor';
            div.appendChild(cur);
          }
          typeNext();
        }
      }, speed);
    }, l.d);
  }
  typeNext();

  // Glitch hue-rotate on the 404
  var el = document.getElementById('e404');
  setInterval(function() {
    el.style.filter = 'hue-rotate(' + Math.floor(Math.random() * 60) + 'deg)';
    setTimeout(function() { el.style.filter = ''; }, 80);
  }, 2400);

  // Click to shake
  el.addEventListener('click', function() {
    el.classList.add('shaking');
    setTimeout(function() { el.classList.remove('shaking'); }, 400);
  });
})();
</script>
